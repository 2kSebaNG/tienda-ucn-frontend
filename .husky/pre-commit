echo "🟡 Running code formatting and checks..."
npx lint-staged && npx tsc --noEmit --skipLibCheck
if [ $? -ne 0 ]; then
  echo "🔴 Commit blocked: Fix errors before committing"
  exit 1
fi
echo "🟢 All checks passed. Proceeding with commit"
